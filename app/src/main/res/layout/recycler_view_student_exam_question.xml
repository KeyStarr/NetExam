<?xml version="1.0" encoding="utf-8"?>

<RelativeLayout android:id="@+id/item_container"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="@drawable/selector_row"
    xmlns:android="http://schemas.android.com/apk/res/android">

    <TextView
        android:id="@+id/question_number"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:background="?android:attr/selectableItemBackground"
        android:padding="@dimen/exam_recycler_item_text_padding"
        android:textColor="@android:color/black"
        android:textSize="16sp" />

   <View
       android:layout_width="match_parent"
       android:layout_height="@dimen/recycler_view_divider_height"
       android:background="?android:attr/listDivider" />

    <ImageView
        android:id="@+id/ic_question_done"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:src="@drawable/ic_done"
        android:layout_toRightOf="@id/question_number"
        android:layout_toEndOf="@id/question_number"
        android:layout_centerVertical="true"
        android:contentDescription="@string/question_has_your_answer"
        android:visibility="gone"/>

</RelativeLayout>